# 快速修复 - 7个圆点缺少2月5日数据

## 🚀 3步搞定（5分钟）

### 步骤1：诊断（看看缺什么）

在 Supabase SQL Editor 执行：

```sql
-- 文件：diagnose_feb5_simple.sql

-- 查看 daily_history 表（7个圆点显示的数据）
SELECT 
  date as "日期",
  participant_count as "参与人数",
  overtime_count as "加班人数",
  on_time_count as "准点人数"
FROM daily_history
WHERE date >= '2026-01-30' AND date <= '2026-02-06'
ORDER BY date DESC;

-- 查看 status_records 表（原始数据）
SELECT 
  date as "日期",
  COUNT(DISTINCT user_id) as "参与人数"
FROM status_records
WHERE date >= '2026-01-30' AND date <= '2026-02-06'
GROUP BY date
ORDER BY date DESC;
```

---

### 步骤2：修复 2月5日

在 Supabase SQL Editor 执行：

```sql
-- 文件：fix_feb5_daily_archive.sql

-- 手动归档 2月5日的数据
SELECT * FROM archive_daily_data('2026-02-05'::DATE);
```

**刷新应用，2月5日的圆点应该显示出来了！**

---

### 步骤3：永久解决（配置自动归档）

在 Supabase SQL Editor 执行：

```sql
-- 文件：setup_workday_archive_cron_beijing.sql

-- 启用 pg_cron 扩展
CREATE EXTENSION IF NOT EXISTS pg_cron;

-- 删除旧任务（如果存在）
SELECT cron.unschedule('daily-archive') WHERE EXISTS (
  SELECT 1 FROM cron.job WHERE jobname = 'daily-archive'
);

-- 创建每日归档任务（每天 00:00 北京时间）
SELECT cron.schedule(
  'daily-archive',
  '0 16 * * *',  -- 16:00 UTC = 00:00 北京时间
  $$
  SELECT archive_daily_data((CURRENT_DATE AT TIME ZONE 'UTC' AT TIME ZONE 'Asia/Shanghai' - INTERVAL '1 day')::DATE);
  $$
);

-- 验证任务是否创建成功
SELECT 
  jobid as "任务ID",
  jobname as "任务名称",
  schedule as "执行计划",
  active as "是否激活"
FROM cron.job
WHERE jobname = 'daily-archive';
```

**预期结果：**
```
任务ID | 任务名称        | 执行计划      | 是否激活
2      | daily-archive  | 0 16 * * *   | true
```

---

## ✅ 完成！

现在你有：
- ✅ 2月5日的数据已修复
- ✅ Supabase Cron 每天 00:00 自动归档
- ✅ GitHub Actions 每天 06:00 备份归档
- ✅ 双重保障，再也不会缺数据了

---

## 📊 验证一下

查看所有 Cron 任务：

```sql
SELECT 
  jobid as "任务ID",
  jobname as "任务名称",
  schedule as "执行计划",
  active as "是否激活"
FROM cron.job
WHERE jobname IN ('daily-archive', 'hourly-snapshot');
```

**应该看到 2个任务：**
```
任务ID | 任务名称          | 执行计划      | 是否激活
1      | hourly-snapshot  | 0 * * * *    | true    ← 每小时快照
2      | daily-archive    | 0 16 * * *   | true    ← 每日归档
```

---

## 🎉 搞定！

以后再也不用担心 7个圆点缺数据了！
