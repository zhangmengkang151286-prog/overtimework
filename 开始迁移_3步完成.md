# 🚀 开始迁移 - 3 步完成

## 准备工作已完成 ✅

我已经为你创建了所有需要的文件：
- ✅ `src/services/postgrestApi.ts` - PostgREST API 基础封装
- ✅ `src/services/dataService.ts` - 数据服务层
- ✅ `src/services/supabaseService.new.ts` - 更新后的数据服务
- ✅ `src/services/supabaseRealtimeService.new.ts` - 更新后的实时服务

---

## 🎯 立即执行（3 步）

### 步骤 1：备份（1 分钟）

在 PowerShell 中执行：

```powershell
cd OvertimeIndexApp

# 创建备份
New-Item -ItemType Directory -Force -Path .backup/services
Copy-Item src/services/supabase.ts .backup/services/
Copy-Item src/services/supabaseService.ts .backup/services/
Copy-Item src/services/supabaseRealtimeService.ts .backup/services/

Write-Host "✅ 备份完成" -ForegroundColor Green
```

### 步骤 2：替换文件（1 分钟）

```powershell
# 替换核心服务文件
Copy-Item src/services/supabaseService.new.ts src/services/supabaseService.ts -Force
Copy-Item src/services/supabaseRealtimeService.new.ts src/services/supabaseRealtimeService.ts -Force

Write-Host "✅ 文件已替换" -ForegroundColor Green
```

### 步骤 3：测试（5 分钟）

```powershell
# 清除缓存并启动
npx expo start --clear
```

在手机上：
1. 打开 Expo Go
2. 扫描二维码
3. 查看是否还有 JWT 错误

**预期结果：**
- ✅ 不再出现 "Server lacks JWT secret" 错误
- ✅ 应用正常运行
- ✅ 数据正常加载

---

## ✅ 如果测试成功

恭喜！迁移完成！

### 清理工作

```powershell
# 删除 .new 文件
Remove-Item src/services/supabaseService.new.ts
Remove-Item src/services/supabaseRealtimeService.new.ts

# 卸载 Supabase SDK（可选）
npm uninstall @supabase/supabase-js

Write-Host "✅ 迁移完成！" -ForegroundColor Green
```

---

## ❌ 如果测试失败

不用担心，可以立即回滚：

```powershell
# 恢复备份
Copy-Item .backup/services/* src/services/ -Force

# 重启应用
npx expo start --clear

Write-Host "✅ 已恢复到迁移前状态" -ForegroundColor Yellow
```

然后告诉我遇到的错误，我会帮你解决。

---

## 📝 注意事项

1. **确保 ECS 上的 PostgREST 正常运行**
   ```bash
   # 在 ECS 上测试
   curl http://localhost/api/tags
   ```

2. **确保 .env 文件配置正确**
   ```env
   SUPABASE_URL=http://121.89.95.95/api
   ```

3. **如果遇到网络错误**
   - 检查 ECS 安全组是否开放 80 端口
   - 检查 PostgREST 容器是否运行
   - 测试公网访问：`curl http://121.89.95.95/api/tags`

---

## 🎉 就这么简单！

只需要 3 步，7 分钟，就可以完成整个迁移！

**准备好了吗？** 从步骤 1 开始执行！
